# IClude MCP server (development mode)
# 开发模式配置，需先启动 go run ./cmd/mcp/

[mcp_servers.iclude]
command = "go"
args = ["run", "./cmd/mcp/", "--stdio"]
startup_timeout_sec = 30
tool_timeout_sec = 60
enabled = true
