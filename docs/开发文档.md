# IClude 记忆体系统开发框架文档

## 1. 项目定位与核心原则

### 1.1 一句话定义
IClude 是一个开源、本地优先、混合存储的企业级记忆体系统，专注于为 AI 应用提供记忆的存储、检索与思考型检索能力，不涉及生成、外部工具或用户界面。

### 1.2 核心范围（做什么）
- ✅ 记忆存储：支持结构化元数据 + 向量嵌入的混合存储（SQLite + Qdrant）
- ✅ 记忆检索：向量检索、全文检索、图谱关联检索、三路 RRF 混合融合
- ✅ 记忆反思（Reflect）：调用外部 LLM 对已有记忆做多步推理，自动生成新洞察（kind=mental_model）
- ✅ 自动知识抽取（Retain）：写入记忆时可选由 LLM 自动提取实体/关系/时间，写入知识图谱
- ✅ 记忆演化：事实(fact) → 经验(observation) → 心智模型(mental_model) 三层自动演化
- ✅ 记忆生命周期：5 级保留策略 + 指数衰减 + 强化 + 软删除/恢复 + 过期清理
- ✅ 层级上下文：树形 Context 结构（物化路径），支持行为配置（mission/directives/disposition）
- ✅ 知识图谱：实体 + 关系 + 记忆-实体关联，支持图谱遍历检索
- ✅ 文档处理：上传 → 分块 → 记忆化管道
- ✅ 分区治理：多团队数据隔离，支持跨团队统一检索
- ✅ 开放 API + SDK + MCP：RESTful API、Python/Go/Node.js SDK、MCP Server（Claude/Gemini 直接调用）

### 1.3 明确不做的范围（不做什么）
- ❌ 不生成答案：调用外部 LLM 仅用于知识抽取和记忆反思，不生成面向用户的自然语言回答
- ❌ 不执行外部工具：不集成代码执行、网络搜索、API 调用等
- ❌ 不内置 AI 模型：用户自行配置 API Key 或本地模型（支持 OpenAI/Anthropic/Ollama 等）

### 1.4 设计哲学
- 本地优先：数据默认存储在用户自己的机器或服务器，一个二进制文件搞定
- 透明可审计：数据格式开放，支持人工检查和迁移
- 渐进式复杂：从单机单文件开始，按需演进至分布式高可用
- 一人可维护：所有设计考虑单人开发与运维成本

## 2. 整体架构图

> 竞品参照：[vectorize-io/hindsight](https://github.com/vectorize-io/hindsight)（Retain/Recall/Reflect 三大操作）

```
┌──────────────────────────────────────────────────────────────────┐
│              Client SDK (Python/Go/Node.js) + MCP Server         │
└──────────────────────────┬───────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                     IClude Core Service                           │
├──────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                   API Layer (RESTful + MCP)                 │  │
│  │  /v1/memories        记忆 CRUD + 软删除/恢复/强化           │  │
│  │  /v1/retrieve        单轮检索（三路 RRF + Reranker）        │  │
│  │  /v1/reflect         反思推理（多步 LLM 推理 + 溯源链）     │  │
│  │  /v1/conversations   对话摄取                               │  │
│  │  /v1/contexts        层级上下文树                            │  │
│  │  /v1/tags            标签系统                               │  │
│  │  /v1/entities        知识图谱（实体/关系/关联）              │  │
│  │  /v1/documents       文档上传/分块/记忆化                    │  │
│  │  /v1/timeline        时间线查询                             │  │
│  │  /v1/maintenance     运维（过期清理/硬删除）                 │  │
│  └────────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                  Business Logic Layer                       │  │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐       │  │
│  │  │Memory Manager│ │Context       │ │Graph         │       │  │
│  │  │(CRUD+双写+   │ │Manager       │ │Manager       │       │  │
│  │  │ 标签+生命周期)│ │(树形结构)    │ │(知识图谱)    │       │  │
│  │  └──────────────┘ └──────────────┘ └──────────────┘       │  │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐       │  │
│  │  │Retriever     │ │Reflect       │ │Document      │       │  │
│  │  │(三路RRF+     │ │Engine [P2]   │ │Processor     │       │  │
│  │  │ Reranker[P3])│ │(多步推理)    │ │(分块+记忆化) │       │  │
│  │  └──────────────┘ └──────────────┘ └──────────────┘       │  │
│  │  ┌──────────────┐ ┌──────────────┐                        │  │
│  │  │Extractor [P2]│ │Lifecycle     │                        │  │
│  │  │(自动实体抽取)│ │(衰减+强化)   │                        │  │
│  │  └──────────────┘ └──────────────┘                        │  │
│  └────────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                 Storage Abstraction Layer                   │  │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐             │  │
│  │  │MemoryStore │ │VectorStore │ │ContextStore│             │  │
│  │  │(31列+FTS5) │ │(Qdrant)    │ │(树+路径)   │             │  │
│  │  └────────────┘ └────────────┘ └────────────┘             │  │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐             │  │
│  │  │ TagStore   │ │ GraphStore │ │DocumentStore│            │  │
│  │  │(多对多)    │ │(实体+关系) │ │(哈希去重)  │             │  │
│  │  └────────────┘ └────────────┘ └────────────┘             │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
       │                           │
       ▼                           ▼
┌──────────────┐          ┌────────────────────┐
│ SQLite 文件  │          │   Qdrant (可选)    │
│ iclude.db    │          │   本地容器/远程    │
│ (9表+1FTS5)  │          │                    │
└──────────────┘          └────────────────────┘
```

> `[P2]` = Phase 2 实现，`[P3]` = Phase 3 实现，无标记 = Phase 1 已完成

## 3. 核心模块说明

### 3.1 存储抽象层（Storage Abstraction Layer）
职责：定义统一的存储接口，隔离底层实现。

#### 接口定义（Go 示例）：

```go
type MemoryStore interface {
    Create(mem *Memory) error
    Get(id string) (*Memory, error)
    Update(mem *Memory) error
    Delete(id string) error
    SearchText(query string, limit int) ([]*Memory, error) // 全文检索
}

type VectorStore interface {
    Insert(memoryID string, embedding []float32, payload map[string]interface{}) error
    Delete(memoryID string) error
    Search(embedding []float32, limit int) ([]SearchResult, error)
}
```

#### 实现：
- SQLiteStore：基于 SQLite，负责元数据、全文检索（FTS5）、实体关系表。
- QdrantStore：基于 Qdrant REST API，负责向量存储与检索。

#### 配置开关：通过环境变量或配置文件控制是否启用每个后端。

### 3.2 业务逻辑层

#### 3.2.1 Memory Manager（✅ Phase 1 已完成）
- 记忆 CRUD，根据配置同时写入 SQLite 和/或 Qdrant（最佳努力双写）
- 创建时支持可选字段：context_id, kind, scope, tags, retention_tier 等
- 标签关联：find-or-create + 记忆-标签多对多
- 软删除/恢复 + Context memory_count 维护
- 对话摄取（IngestConversation）：批量写入 + 自动 turn_number

#### 3.2.2 Retriever（✅ Phase 1 已完成，Phase 2~3 增强）

**Phase 1 — 双路 RRF：**
- 三模式：SQLite-only / Qdrant-only / Hybrid RRF(k=60)
- 强度加权：结果分数 × effective_strength（指数衰减）
- 过期过滤 + SearchFilters 多条件过滤

**Phase 2 — 三路 RRF + Token 裁剪 [B3][B5]：**
- 新增第三路：Graph 实体关联检索（查询→实体识别→图谱遍历→关联记忆）
- Token 感知裁剪（max_tokens 参数，按预算截断）

**Phase 3 — Cross-encoder 重排 [B4]：**
- RRF 融合后可选调用 Reranker 精排
- 完整管道：三路检索 → RRF → Reranker → 强度加权 → Token 裁剪

#### 3.2.3 Context Manager（✅ Phase 1 已完成）
- 树形上下文管理（物化路径），支持 project / topic / session 三种类型
- CRUD + ListChildren + ListSubtree + Move（含循环引用检测）
- Phase 3 扩展：行为配置（mission/directives/disposition）[B7]

#### 3.2.4 Graph Manager（✅ Phase 1 已完成，Phase 2 增强）
- Entity/Relation/MemoryEntity 全套 CRUD
- Phase 2 增强：Extractor 自动抽取（Create 时 auto_extract=true）[B2]

#### 3.2.5 Reflect Engine（Phase 2 新增）[B1]

借鉴 Hindsight 的 Reflect 能力，与原规划"多轮思考型检索"合并设计。

- 输入：用户问题、scope、最大轮数、token 预算
- 过程：
  1. 召回相关记忆（调用 Retriever）
  2. 调用外部 LLM 分析记忆间关联，判断是否需要更多信息
  3. 若需要，生成新查询关键词并再次检索
  4. 循环直到满足条件或达到最大轮数
  5. 最终推理结果自动写回为新记忆（kind=mental_model, source_type=reflect）
- 输出：

```json
{
  "result": "多步推理得出的综合分析",
  "new_memory_id": "自动生成的心智模型记忆 ID",
  "trace": [
    {
      "round": 1,
      "query": "原始问题",
      "retrieved": ["mem1", "mem2"],
      "llm_reasoning": "发现 mem1 和 mem2 存在因果关系..."
    }
  ],
  "sources": ["mem1", "mem2", "mem3"],
  "metadata": {
    "rounds_used": 2,
    "total_tokens": 3200
  }
}
```

#### 3.2.6 Extractor（Phase 2 新增）[B2]

自动知识抽取器，写入记忆时由 LLM 提取结构化知识。

- 接口：`Extract(ctx, content) → ([]Entity, []Relation, error)`
- 实现：LLM Extractor（调用外部 LLM，prompt 工程提取实体/关系/时间）
- 实体规范化：不同说法的同一实体合并为统一节点
- 集成点：`manager.Create(mem, auto_extract=true)` 时自动调用

#### 3.2.7 Lifecycle Manager（✅ Phase 1 已完成）
- 5 级保留策略：permanent / long_term / standard / short_term / ephemeral
- 指数衰减：`effective = strength × e^(-decay_rate × hours)`
- 强化公式：`strength += 0.1 × (1 - strength)`
- 过期清理 + 硬删除级联（memory_tags + memory_entities）

#### 3.2.8 Document Processor（✅ Phase 1 已完成）
- 上传 → 段落分块(1000字) → 批量创建记忆
- SHA256 内容哈希去重
- 状态管理：pending → processing → ready / failed

**重要：** IClude 自身不包含任何 AI 模型，Reflect/Extractor/Embedder 均调用外部 LLM（用户提供 API Key 或本地 Ollama）。

### 3.3 API 层（✅ Phase 1 已完成）
- 使用 Gin 实现 RESTful API，所有端点在 `/v1/` 下
- 统一 JSON 响应格式 + 13 种错误映射
- CORS + 请求日志中间件
- 功能门控：nil check 决定是否注册端点组
- Phase 2 新增：`POST /v1/reflect`
- Phase 3 新增：MCP Server（`cmd/mcp/main.go`）

### 3.4 SDK
- Python SDK：✅ Phase 1 基础版已完成
- Go / Node.js SDK：Phase 2 开发
- MCP Server：Phase 3（让 Claude/Gemini 直接调用 IClude 记忆）
- SDK 核心操作对齐 Hindsight 命名：**retain（存入）/ recall（召回）/ reflect（反思）**

## 4. 数据模型（Phase 1 实际实现）

### 4.1 SQLite 数据库（9 表 + 1 FTS5 虚拟表）

#### memories 表（31 列）

```
原有 10 列
├── id, content, metadata, team_id
├── embedding_id, parent_id, is_latest, access_count
└── created_at, updated_at

+ 7 列 — 分层扩展
├── uri                  iclude://{scope}/{path}#{id}
├── context_id           FK → contexts.id
├── kind                 note / fact / skill / profile
├── sub_kind             entity / event / pattern / preference / case
├── scope                顶级命名空间
├── abstract             一句话摘要
└── summary              核心信息

+ 5 列 — 时间线与来源
├── happened_at          事件发生时间
├── source_type          manual / conversation / document / api / reflect [P2] / consolidate [P3]
├── source_ref           来源引用标识
├── document_id          FK → documents.id
└── chunk_index          文档分块序号

+ 6 列 — 生命周期
├── deleted_at           软删除时间
├── strength             记忆强度 0~1，默认 1.0
├── decay_rate           衰减速率，默认 0.01
├── last_accessed_at     最近访问时间
├── reinforced_count     强化次数
└── expires_at           过期时间

+ 3 列 — 对话与保留策略
├── retention_tier       permanent/long_term/standard/short_term/ephemeral
├── message_role         user/assistant/system/tool
└── turn_number          对话轮次序号

Phase 3 预留列：
├── layer                记忆演化层级：1(fact) / 2(observation) / 3(mental_model)
└── derived_from         溯源记忆 ID 列表（JSON 数组）
```

#### 其他表

| 表名 | 用途 | 关键约束 |
|------|------|---------|
| `schema_version` | 迁移版本追踪 | PK(version) |
| `contexts` | 层级容器（物化路径树） | UNIQUE(path) |
| `tags` | 标签 | UNIQUE(name, scope) |
| `memory_tags` | 记忆 ↔ 标签 | PK(memory_id, tag_id) |
| `entities` | 知识图谱实体 | UNIQUE(name, entity_type, scope) |
| `entity_relations` | 实体关系 | UNIQUE(source, target, type) |
| `memory_entities` | 记忆 ↔ 实体 | PK(memory_id, entity_id) |
| `documents` | 文档知识库 | content_hash 去重 |

#### FTS5 全文检索

```sql
-- 三列 external content 模式，BM25 加权
CREATE VIRTUAL TABLE memories_fts USING fts5(
    content, abstract, summary,
    content=memories, content_rowid=rowid
);
-- 搜索：bm25(memories_fts, 10, 5, 3)  权重 content=10, abstract=5, summary=3
-- 中文分词：pkg/tokenizer（Simple/Jieba/Noop 可拔插）
```

### 4.2 Qdrant 集合设计

- 集合名称：memories
- 向量维度：可配置（默认 384）

#### Payload：

```json
{
  "memory_id": "uuid",
  "team_id": "team1",
  "created_at": "2026-03-19T12:00:00Z",
  "scope": "tech",
  "kind": "fact",
  "context_id": "ctx-123",
  "abstract": "一句话摘要",
  "retention_tier": "standard",
  "message_role": ""
}
```

- Phase 2 优化：为 scope/context_id/kind 创建 payload index

## 5. 技术栈选型

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 核心语言 | Go | 1.21+ | 高性能、编译单一二进制文件 |
| SDK 语言 | Python | 3.8+ | 优先开发，开发者最多 |
| | Go | 1.21+ | 第二优先级 |
| | Node.js | 16+ | 第三优先级 |
| 主存储 | SQLite | 3.40+ | 内置，零运维 |
| 向量存储 | Qdrant | 1.7+ | 可选，可通过 Docker 运行或使用云服务 |
| 全文检索 | SQLite FTS5 | 内置 | 无需额外组件 |
| HTTP 框架 | Gin | 最新 | 轻量、高性能 |
| 配置管理 | Viper | 最新 | 支持 YAML/环境变量 |
| 日志 | Zap | 最新 | 高性能结构化日志 |
| 测试 | testify | 最新 | 单元测试 |
| 部署 | Docker | 可选 | 提供 Dockerfile 和 docker-compose |

## 6. 阶段化开发计划

> 标记 `[Bx]` 为 Hindsight 竞品对比借鉴项，详见 Phase1 实施报告第十三节。

### Phase 1：MVP 核心验证 ✅ 已完成（2026-03-18）

目标：实现混合存储 + 分层架构 + 生命周期 + 全功能 API。

| # | 任务 | 状态 | 实际情况 |
|---|------|------|---------|
| 1 | 项目结构重组 | ✅ | `business/` → `internal/`，接口扁平化，`pkg/` 抽出可复用组件 |
| 2 | 存储抽象层 | ✅ | 8 个接口（`store/interfaces.go` 7 个 + `Embedder` 在 `embed/`），共 70+ 方法 |
| 3 | SQLite 后端 | ✅ | 31 列 + FTS5 三列 BM25 加权 + V0→V6 迁移（Phase 2 持续扩展）+ 13 索引 |
| 4 | Qdrant 后端 | ✅ | `VectorStore` + `SearchFiltered` + `GetVectors` |
| 5 | 业务逻辑层 | ✅ | Manager + ContextManager + GraphManager + Lifecycle |
| 6 | 检索层 | ✅ | 三模式 Retriever + RRF(k=60) + 强度加权 |
| 7 | 文档处理 | ✅ | 上传→段落分块→记忆化 + SHA256 哈希去重 |
| 8 | HTTP API | ✅ | 45 个端点（Phase 2 持续增加），全功能覆盖 |
| 9 | Python SDK | ✅ | 基础版（CRUD + retrieve），Phase 2 已扩展至全端点覆盖 |
| 10 | 测试 | ✅ | 207 个测试函数，35 个测试文件 + HTML 可视化报告（`pkg/testreport`） |
| 11 | FTS5 中文分词 | ✅ | `pkg/tokenizer`（Simple / Jieba / GSE / Noop 四种，可拔插） |
| 12 | SQL Builder | ✅ | `pkg/sqlbuilder` 替代字符串拼接 |

### Phase 2：智能记忆版 ✅ 已完成（2026-03-25）

目标：从”被动存取”升级为”主动学习”——Reflect 反思 + 自动实体抽取 + 图谱检索 + Token 裁剪。

| # | 任务 | 状态 | 实际情况 |
|---|------|------|---------|
| 1 | Reflect 反思机制 | ✅ | `internal/reflect/engine.go`（独立包避免循环依赖）。多轮 LLM 推理 + 3级 fallback 解析 + query dedup + token budget + auto_save mental_model。端点 `POST /v1/reflect`。每轮 LLM 调用加独立 context.WithTimeout 防泄漏 |
| 2 | Extractor 自动实体抽取 | ✅ | `internal/memory/extractor.go`。Create 时非阻塞 goroutine 提取（含 config.Extract.Timeout 超时保护）+ LLM 提取实体/关系 + 规范化 + 3级 fallback。端点 `POST /v1/memories/:id/extract` |
| 3 | 图谱参与检索 | ✅ | Retriever 第三路：FTS5 匹配实体 → 图谱 BFS 遍历（可配置深度）→ 关联记忆。LLM fallback（FTS5 无结果时）。三路 RRF 融合 + 循环保护 |
| 4 | Token 感知裁剪 | ✅ | `max_tokens` + `detail_level`（abstract_only / summary / full）双重截断。CJK+英文混合 Token 估算（CJK=1token，英文词×1.3） |
| 5 | Python SDK 全端点覆盖 | ✅ | `sdks/python/iclude/client.py` 扩展至 26 个方法，覆盖 reflect/entities/tags/documents/contexts。`reflect()` 字段修正 query→question，`create()` 补全 auto_extract/tags/visibility/scope/kind，`retrieve()` 补全 filters/mmr_enabled/mmr_lambda/detail_level/max_tokens |
| 6 | 对话摄取事务批量写入 | ✅ | IngestConversation 单事务包裹，`POST /v1/conversations` |
| 7 | 异步 AccessTracker | ✅ | `internal/memory/access_tracker.go`，buffered channel（10000 容量）异步批量更新 access_count，通过 `AccessTracker` 接口注入 Retriever 避免循环依赖 |
| 8 | Qdrant payload index | ✅ | `qdrant.Init()` 启动时为 scope/context_id/kind/team_id/owner_id/visibility 创建 keyword 索引，幂等 |
| 9 | detail_level 裁剪 | ✅ | `applyDetailLevel()` 在 search_handler 中裁剪 Content/Summary 字段 |
| 10 | Scheduler + Heartbeat | ✅ | `internal/scheduler/` + `internal/heartbeat/` 已实现并接入 main.go。4 个定时任务：access-flush / cleanup / consolidation / heartbeat-inspect。Heartbeat LLM 调用加独立超时 |
| 11 | Memory Consolidator | ✅ | `internal/memory/consolidator.go`，层级聚类 + LLM 摘要，输出验证（空/过短拒绝），scope/kind/teamID 继承，config-gated（`consolidation.enabled`），LLM 调用加独立 30s 超时 |
| 12 | MMR Reranking API 暴露 | ✅ | `RetrieveRequest.MmrEnabled/MmrLambda` per-request 覆盖全局配置；Retriever 接入 MMR 重排逻辑；Python SDK 同步支持 `mmr_enabled`/`mmr_lambda` 参数 |
| 13 | config.yaml 配置补全 | ✅ | `config.yaml` + `deploy/config.yaml` 补全 access_alpha、mmr、preprocess、scheduler、consolidation、heartbeat 六个 Phase 2 配置节 |
| 14 | LLM 超时统一加固 | ✅ | Reflect 每轮、Extractor goroutine、Consolidator、Heartbeat 矛盾检测、Extractor llmNormalize 均添加独立 context.WithTimeout，防止 goroutine 泄漏 |
| 15 | 性能基准测试 | ✅ | `testing/search/benchmark_test.go`：BenchmarkWeightedRRF_ThreeWay/TwoWay、BenchmarkMMRRerank（n=10/25/50）、BenchmarkEstimateTokens（中英混合） |

#### 超出计划额外完成项：

| 额外项 | 说明 |
|--------|------|
| Go/Node.js SDK | 未实现（Python SDK 已全覆盖，移至 Phase 3 规划） |
| Prometheus 监控 | 未实现（移至 Phase 3 规划） |
| testreport Dashboard Phase 2 | `testing/report/phase2_test.go`：11 个 testreport 可视化用例（MMR/Token/Scheduler/Heartbeat/Consolidation） |

#### Phase 2 完成状态总结：

| 指标 | 数值 |
|------|------|
| 计划任务数 | 11 |
| 实际完成任务数 | 15（含 4 项超计划加固项） |
| 额外新增能力 | Scheduler、Heartbeat、Consolidator、MMR API、LLM 超时加固、性能基准 |
| 未完成（移至 Phase 3） | Go/Node.js SDK、Prometheus 监控 |

### Phase 3：认知进化版（2026-03-26 启动，预计 3-4 个月）

目标：记忆自动演化（事实→经验→心智模型）+ Cross-encoder 精排 + MCP 集成 + 管理后台 + 企业级认证。

> 执行顺序按优先级排列，括号内为预计工期。

#### 3a — 快速交付层（第 1-3 周，高优先）

| # | 任务 | 状态 | 时长 | 来源 | 详细描述 |
|---|------|------|------|------|----------|
| 1 | MCP Server 集成 | ✅ | 2周 | 新增 | `cmd/mcp/main.go`，暴露 retain/recall/reflect/scan/fetch/ingest/timeline 七大 MCP 工具，HTTP+SSE 传输；Claude/Gemini 直接调用 IClude 记忆。依赖：Phase 2 Reflect Engine ✅ |
| 2 | Memory Bank 行为配置 | 🔲 | 1周 | [B7] | Context 节点 metadata 增加 `mission`/`directives`/`disposition` 三字段；Reflect 引擎读取当前 context 配置，注入 system prompt，影响推理风格。依赖：ContextManager ✅ |

#### 3b — 检索增强层（第 4-5 周，中优先）

| # | 任务 | 状态 | 时长 | 来源 | 详细描述 |
|---|------|------|------|------|----------|
| 3 | Cross-encoder 重排 | 🔲 | 2周 | [B4] | 新增 `Reranker` 接口（`internal/search/reranker.go`）。RRF 融合后可选调用 cross-encoder 模型（本地 ONNX 或 OpenAI rerank API）精排 Top-K，结果管道：三路检索 → RRF → Reranker → 强度加权 → Token 裁剪。config-gated |
| 4 | Go SDK | 🔲 | 1周 | 原规划 | `sdks/go/iclude/client.go`，对齐 Python SDK 26 个方法，stdlib only（无第三方依赖） |

#### 3c — 认证与安全层（第 6-7 周，上线前必须）

| # | 任务 | 状态 | 时长 | 来源 | 详细描述 |
|---|------|------|------|------|----------|
| 5 | 认证与权限 | 🔲 | 2周 | 原规划 | JWT/API Key 双模式认证（当前仅 API Key）+ 团队角色（owner/editor/viewer）+ Context 树访问控制。中间件在 `internal/api/middleware.go` 扩展 |

#### 3d — 记忆演化层（第 8-10 周，核心差异化）

| # | 任务 | 状态 | 时长 | 来源 | 详细描述 |
|---|------|------|------|------|----------|
| 6 | 记忆自动演化链 | 🔲 | 3周 | [B6] | 三层演化：fact(1) → observation(2) → mental_model(3)。新增 `layer`（int）+ `derived_from`（JSON 数组）字段（SQLite 迁移 V7）。Consolidator 触发 observation 层；Reflect auto_save 触发 mental_model 层。演化历史可溯源 |
| 7 | LongMemEval 基准测试 | 🔲 | 2周 | 新增 | 建立可复现评测流程（`testing/eval/`），采用 LongMemEval 数据集，对标 Hindsight 91.4% 准确率。评测维度：单跳/多跳/时间敏感/矛盾检测 |
| 7.5 | 标签体系升级 | 🔲 | 2周 | claude-mem 借鉴 | Tag 模型增加 `category` 字段（domain/attribute/role/topic），支持多维度分类（领域知识、属性标记、职能角色等）；增加 `parent_id` 支持层级标签（如 AI > NLP > RAG）；迁移 V8。为未来不同领域/属性/职能的知识管理奠定基础 |

#### 3e — 可视化与运维层（第 11-13 周，体验提升）

| # | 任务 | 状态 | 时长 | 来源 | 详细描述 |
|---|------|------|------|------|----------|
| 8 | 管理后台 | 🔲 | 3周 | 原规划 | React/Vue Web 后台（`tools/admin-ui/`）：知识图谱可视化（d3.js force-directed）+ 记忆演化链路追踪 + 数据导入导出（JSON/CSV）。依赖认证模块 |
| 9 | Prometheus 监控 | 🔲 | 1周 | 原规划 | `/metrics` 端点暴露：检索延迟直方图、LLM 调用次数/耗时、记忆数量趋势、Scheduler 任务执行状态 |
| 10 | 性能与稳定性 | 🔲 | 2周 | 原规划 | 压力测试（10 万条记忆，P99 ≤ 200ms 单轮检索）；SQLite WAL 调优；连接池参数回归；Qdrant 向量索引调优；Go pprof 热点分析 |

#### Phase 3 交付物：

| 交付物 | 状态 |
|--------|------|
| MCP Server（Claude/Gemini 直接调用） | ✅ |
| Memory Bank 行为配置（mission/directives） | 🔲 |
| Cross-encoder 重排（可选精排管道） | 🔲 |
| Go SDK | 🔲 |
| 认证与权限（JWT + 角色 + Context 权限） | 🔲 |
| 三层记忆自动演化（fact→observation→mental_model） | 🔲 |
| 标签体系升级（多维分类 + 层级标签） | 🔲 |
| LongMemEval 基准评测结果（目标 ≥ 80%） | 🔲 |
| 管理后台（图谱可视化 + 演化追踪） | 🔲 |
| Prometheus 监控端点 | 🔲 |

#### Phase 3 里程碑：

| 时间节点 | 里程碑 |
|----------|--------|
| W3（第 3 周） | MCP Server 可用，Claude Desktop 可直接调用 IClude |
| W7（第 7 周） | 认证上线，可公开部署第一个生产实例 |
| W10（第 10 周） | 三层演化链路跑通，LongMemEval 初版评测结果出炉 |
| W13（第 13 周） | 管理后台 Beta 可用，Phase 3 全部交付 |

## 7. API 设计概览（RESTful）

### 已实现端点（Phase 1）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/v1/memories` | POST | 创建记忆（支持 context_id/kind/scope/tags/retention_tier/auto_extract[P2]） |
| `/v1/memories` | GET | 列表（支持 scope/context_id/kind/tags 过滤） |
| `/v1/memories/:id` | GET | 获取详情（自动递增 access_count） |
| `/v1/memories/:id` | PUT | 更新 |
| `/v1/memories/:id` | DELETE | 软删除 |
| `/v1/memories/:id/soft` | DELETE | 显式软删除 |
| `/v1/memories/:id/restore` | POST | 恢复软删除 |
| `/v1/memories/:id/reinforce` | POST | 强化（strength += 0.1 × (1-strength)） |
| `/v1/memories/:id/tags` | GET/POST | 获取/打标签 |
| `/v1/memories/:id/tags/:tag_id` | DELETE | 移除标签 |
| `/v1/retrieve` | POST | 检索（三模式 RRF + filters + 强度加权） |
| `/v1/timeline` | GET | 时间线查询（scope/after/before/limit） |
| `/v1/conversations` | POST | 对话摄取（批量创建 + auto context） |
| `/v1/conversations/:ctx_id` | GET | 获取对话记录（turn_number 排序） |
| `/v1/contexts` | POST/GET | 创建/列表 |
| `/v1/contexts/:id` | GET/PUT/DELETE | 获取/更新/删除 |
| `/v1/contexts/:id/children` | GET | 子节点 |
| `/v1/contexts/:id/tree` | GET | 子树 |
| `/v1/contexts/:id/move` | POST | 移动 |
| `/v1/tags` | POST/GET | 创建/列表 |
| `/v1/tags/:id` | DELETE | 删除 |
| `/v1/entities` | POST/GET | 创建/列表 |
| `/v1/entities/:id` | GET/PUT/DELETE | 获取/更新/删除 |
| `/v1/entities/:id/relations` | GET | 关系列表 |
| `/v1/entities/:id/memories` | GET | 关联记忆 |
| `/v1/entity-relations` | POST | 创建关系 |
| `/v1/entity-relations/:id` | DELETE | 删除关系 |
| `/v1/memory-entities` | POST/DELETE | 记忆-实体关联/解除 |
| `/v1/documents` | POST/GET | 上传/列表 |
| `/v1/documents/:id` | GET/DELETE | 获取/删除 |
| `/v1/documents/:id/reprocess` | POST | 重新处理 |
| `/v1/maintenance/cleanup` | POST | 清理过期记忆 |

### Phase 2 新增端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/reflect` | POST | 反思推理（question + scope + max_rounds + max_tokens） |

### Phase 3 新增

| 端点 | 方法 | 说明 |
|------|------|------|
| MCP Server | stdio/http | retain/recall/reflect 三大 MCP 工具 |

## 8. 配置与部署

### 8.1 配置文件示例（YAML）

```yaml
storage:
  sqlite:
    enabled: true
    path: "./data/iclude.db"
  qdrant:
    enabled: true
    url: "http://localhost:6333"
    collection: "memories"
    dimension: 384

partitions:
  enabled: true
  catalog_path: "./data/partitions.db"  # 分区目录库

llm:  # 多轮检索所需
  default_provider: "openai"
  openai:
    api_key: "${OPENAI_API_KEY}"
    model: "gpt-4"

server:
  port: 8080
  auth_enabled: false  # Phase 3 启用
```

### 8.2 部署方式
- 单二进制文件：直接运行，SQLite 文件生成在指定目录。
- Docker 单容器：将核心服务和 Qdrant 用 docker-compose 一起启动。
- Kubernetes：提供 Helm Chart（Phase 3 后）。

## 9. 成功指标与风险控制

| 维度 | 指标 | 阶段 |
|------|------|------|
| 功能完整性 | 完成各 Phase 所有交付物 | 各阶段 |
| 性能 | 单轮检索 P99 延迟 ≤ 200ms（10 万条记忆） | Phase 1 ✅ |
| | Reflect 平均完成时间 ≤ 5 秒（3 轮） | Phase 2 |
| | 三路 RRF 检索 P99 ≤ 300ms | Phase 2 |
| 记忆质量 | LongMemEval 准确率 ≥ 80%（对标 Hindsight 91.4%） | Phase 3 |
| 代码质量 | 单元测试覆盖率 ≥ 80%（当前 58+ 测试函数） | 各阶段 |
| 社区健康 | GitHub Stars ≥ 100/500/2000（Phase 1/2/3） | 各阶段 |
| | Contributors ≥ 5 | Phase 2+ |
| 商业验证 | 种子用户反馈 ≥ 10 条 | Phase 1 |
| | 企业试用客户 ≥ 3 | Phase 3 |

### 风险控制：
- 技术风险：每个阶段都有可运行版本，不跳过测试。LLM 调用失败不影响核心存取。
- 性能风险：Reflect/Extractor 依赖外部 LLM，设置超时和重试。图谱检索可配置开关。
- 成本风险：LLM 调用按需付费，auto_extract 和 reflect 均为可选功能，不强制。
- 市场风险：对齐 Hindsight 的 retain/recall/reflect 命名，降低用户迁移门槛。

---

**文档版本**：v4.0
**最后更新**：2026-03-25
**变更记录**：
- v4.0 (2026-03-25)：Phase 2 标记完成（15 项任务含 4 项超计划加固）；Phase 3 重新规划为 5 层 10 任务，按优先级排序并设置里程碑节点；更新 Python SDK 完成情况、MMR API 暴露、LLM 超时加固、性能基准测试等实际完成情况
- v3.0 (2026-03-19)：Phase 1 标记完成；融合 Hindsight 竞品 7 项借鉴（B1~B7）到 Phase 2~3；更新架构图、数据模型、API 端点、核心模块说明
- v2.0 (2026-03-18)：Phase 1b 数据库分层完成
- v1.0 (2026-03-08)：初始版本